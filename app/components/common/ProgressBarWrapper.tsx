"use client";

import { AppProgressBar as ProgressBar } from "next-nprogress-bar";

const ProgressBarWrapper = () => {
  return <ProgressBar color="dodgerblue" />;
};

export default ProgressBarWrapper;
